import React from "react";

import { SlidePresentationContainer } from "./layout/SlidePageElements";

const Content = (props) => {
  return <SlidePresentationContainer image={props.image} />;
};

export default Content;
